		include
		define
		auto
		break
		case
		char
		const
		continue
		default
		do
		double
		else
		enum
		extern
		float
		for
		goto
		if
		int
		long
		register
		return
		short
		signed
		sizeof
		static
		struct
		switch
		typedef
		union
		unsigned
		void
		volatile
		while

		comments
		integers
		decimal point
		exponent (E)

binaryOp 	+ - / * %  =  
relOP 		== < > <= >= !=
logicalOP 	&& ||
bitwiseOP	! & | ^(XOR)
rLP	( rRP )
cLP {  cRP } 
sLP [ sRP ]
	
delim			; ,

bitShift		<< >>
string 		"string beween quotes"
charLit		'c'
lineComment
blockComment
id




			token classes - string 
			#include
			#define
			#include 
			<stdio.h> interpret as string
